<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>Runtime on Luenci</title>
    <link>https://luenci.com/en/categories/runtime/</link>
    <description>Recent content in Runtime on Luenci</description>
    <generator>Hugo -- 0.145.0</generator>
    <language>en</language>
    <lastBuildDate>Wed, 11 Mar 2026 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://luenci.com/en/categories/runtime/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>转载 | 理解 Go 运行时（二）：内存分配器</title>
      <link>https://luenci.com/en/posts/%E7%90%86%E8%A7%A3-go-%E8%BF%90%E8%A1%8C%E6%97%B6%E4%BA%8C%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%99%A8/</link>
      <pubDate>Wed, 11 Mar 2026 00:00:00 +0000</pubDate>
      <guid>https://luenci.com/en/posts/%E7%90%86%E8%A7%A3-go-%E8%BF%90%E8%A1%8C%E6%97%B6%E4%BA%8C%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%99%A8/</guid>
      <description>&lt;blockquote&gt;
&lt;p&gt;原文链接：https://internals-for-interns.com/posts/go-memory-allocator/&lt;/p&gt;&lt;/blockquote&gt;
&lt;p&gt;​	你可以将内存分配器想象成一位仓库管理员。你的程序持续需要各种尺寸的&amp;quot;箱子&amp;quot;——有时极小，有时极大——并且需要快速获取。分配器的职责就是尽可能迅速地分发这些箱子，保持仓库井然有序以避免浪费，同时与垃圾回收器协同回收那些不再被使用的箱子。&lt;/p&gt;</description>
      <content:encoded><![CDATA[<blockquote>
<p>原文链接：https://internals-for-interns.com/posts/go-memory-allocator/</p></blockquote>
<p>​	你可以将内存分配器想象成一位仓库管理员。你的程序持续需要各种尺寸的&quot;箱子&quot;——有时极小，有时极大——并且需要快速获取。分配器的职责就是尽可能迅速地分发这些箱子，保持仓库井然有序以避免浪费，同时与垃圾回收器协同回收那些不再被使用的箱子。</p>
<h2 id="内存分配何时发生">内存分配何时发生？</h2>
<p>​	并非程序中的每个变量都会经过内存分配器。Go 语言有两个存放数据的地方：栈和堆。</p>
<p>​	栈是较为简单的部分。每次函数调用都会在栈上获得一小块临时空间，当函数返回时，这块空间会自动消失。这种方式快速且简单——无需记录管理。</p>
<p>​	但有时数据需要在创建它的函数结束后继续存在。可能是返回某个对象的指针，或是存储程序其他部分稍后会使用的值。这类数据不能存放在栈上——因为函数返回时它们会随之消失。所以它们会被放在堆上，这是一个生命周期更长的内存区域。</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 3
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 4
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 5
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 6
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 7
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 8
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 9
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">10
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">11
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">12
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">13
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">14
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">15
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">16
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#ff7b72">package</span> main
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ff7b72">import</span> (
</span></span><span style="display:flex;"><span>	<span style="color:#a5d6ff">&#34;fmt&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ff7b72">func</span> <span style="color:#d2a8ff;font-weight:bold">main</span>() {
</span></span><span style="display:flex;"><span>	a <span style="color:#ff7b72;font-weight:bold">:=</span> <span style="color:#a5d6ff">5</span>
</span></span><span style="display:flex;"><span>	b <span style="color:#ff7b72;font-weight:bold">:=</span> <span style="color:#a5d6ff">10</span>
</span></span><span style="display:flex;"><span>	result <span style="color:#ff7b72;font-weight:bold">:=</span> <span style="color:#d2a8ff;font-weight:bold">Add</span>(a, b)
</span></span><span style="display:flex;"><span>	fmt.<span style="color:#d2a8ff;font-weight:bold">Printf</span>(<span style="color:#a5d6ff">&#34;The sum is %d\n&#34;</span>, result)
</span></span><span style="display:flex;"><span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ff7b72">func</span> <span style="color:#d2a8ff;font-weight:bold">Add</span>(a, b <span style="color:#ff7b72">int</span>) <span style="color:#ff7b72">int</span> {
</span></span><span style="display:flex;"><span>	<span style="color:#ff7b72">return</span> a <span style="color:#ff7b72;font-weight:bold">+</span> b
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></td></tr></table>
</div>
</div><div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 3
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 4
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 5
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 6
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 7
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 8
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 9
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">10
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">11
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">12
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>❯ go run -gcflags<span style="color:#ff7b72;font-weight:bold">=</span><span style="color:#a5d6ff">&#39;-m=2 -l&#39;</span> main.go  <span style="color:#8b949e;font-style:italic"># 关闭内联优化</span>
</span></span><span style="display:flex;"><span><span style="color:#8b949e;font-style:italic"># command-line-arguments</span>
</span></span><span style="display:flex;"><span>./main.go:11:32: result escapes to heap in main:
</span></span><span style="display:flex;"><span>./main.go:11:32:   flow: <span style="color:#ff7b72;font-weight:bold">{</span>storage <span style="color:#ff7b72">for</span> ... argument<span style="color:#ff7b72;font-weight:bold">}</span> ← &amp;<span style="color:#ff7b72;font-weight:bold">{</span>storage <span style="color:#ff7b72">for</span> result<span style="color:#ff7b72;font-weight:bold">}</span>:
</span></span><span style="display:flex;"><span>./main.go:11:32:     from result <span style="color:#ff7b72;font-weight:bold">(</span>spill<span style="color:#ff7b72;font-weight:bold">)</span> at ./main.go:11:32
</span></span><span style="display:flex;"><span>./main.go:11:32:     from ... argument <span style="color:#ff7b72;font-weight:bold">(</span>slice-literal-element<span style="color:#ff7b72;font-weight:bold">)</span> at ./main.go:11:12
</span></span><span style="display:flex;"><span>./main.go:11:32:   flow: <span style="color:#ff7b72;font-weight:bold">{</span>heap<span style="color:#ff7b72;font-weight:bold">}</span> ← <span style="color:#ff7b72;font-weight:bold">{</span>storage <span style="color:#ff7b72">for</span> ... argument<span style="color:#ff7b72;font-weight:bold">}</span>:
</span></span><span style="display:flex;"><span>./main.go:11:32:     from ... argument <span style="color:#ff7b72;font-weight:bold">(</span>spill<span style="color:#ff7b72;font-weight:bold">)</span> at ./main.go:11:12
</span></span><span style="display:flex;"><span>./main.go:11:32:     from fmt.Printf<span style="color:#ff7b72;font-weight:bold">(</span><span style="color:#a5d6ff">&#34;The sum is %d\n&#34;</span>, ... argument...<span style="color:#ff7b72;font-weight:bold">)</span> <span style="color:#ff7b72;font-weight:bold">(</span>call parameter<span style="color:#ff7b72;font-weight:bold">)</span> at ./main.go:11:12
</span></span><span style="display:flex;"><span>./main.go:11:12: ... argument does not escape
</span></span><span style="display:flex;"><span>./main.go:11:32: result escapes to heap  <span style="color:#8b949e;font-style:italic"># 结果返回逃逸到了堆上</span>
</span></span><span style="display:flex;"><span>The sum is <span style="color:#a5d6ff">15</span>
</span></span></code></pre></td></tr></table>
</div>
</div><p>​	Go 编译器在这方面其实相当智能。它会在编译时分析你的代码，决定哪些数据可以留在栈上，哪些需要放到堆上——这被称为<a href="https://internals-for-interns.com/posts/the-go-ir/">逃逸分析</a>。</p>
<p>​	每当有数据被分配到堆上时，内存分配器便开始发挥作用。这个系统负责在堆上寻找可用空间并将其交付使用。这正是本文后续要探讨的核心内容。</p>
<blockquote>
<p>需要稍作简化说明：在 Go 语言中，协程栈实际上是从堆中分配的——因此内存分配器为栈提供了生存空间。但栈一旦被分配，其上的变量管理与堆对象截然不同：这些变量只是栈帧内的偏移量，每个变量都不需要分配器介入管理。所以虽然分配器负责栈内存的分配，但并不参与栈上具体变量的定位。本文将重点聚焦于堆内存的管理机制。</p></blockquote>
<p>​	那么分配器管理着堆内存。但最初这些内存究竟从何而来呢？</p>
<h2 id="为何不直接向操作系统申请">为何不直接向操作系统申请？</h2>
<p>​	当你的程序需要内存时，必须有人提供它。最终，这个提供者就是操作系统。操作系统管理着你机器上的所有物理 RAM，任何需要内存的进程都必须通过系统调用来向操作系统申请，比如在 Linux/macOS 上使用<code>mmap</code>，或在 Windows 上使用<code>VirtualAlloc</code>。</p>
<p>​	问题在于系统调用速度很慢。它们涉及从用户空间切换到内核空间，操作系统进行自己的簿记，然后再切换回来。如果 Go 每次你写<code>new</code>或<code>make</code>时都进行系统调用，性能将会非常糟糕——尤其是在一种为高并发设计的语言中，成千上万的 goroutine 可能同时分配内存。</p>
<p>​	因此，Go 运行时采用了不同的方法：它预先向操作系统申请大块内存（我们稍后会看到，在大多数 64 位系统上这些块是 64MB），然后在内部管理分配。当你的代码需要 100 字节时，分配器不会去找操作系统——它会从已经拥有的内存中切出 100 字节。只有当内存耗尽时，它才会再次向操作系统申请。</p>
<p>​	这就是内存分配器背后的核心理念。它位于你的程序与操作系统之间，充当一个快速中介，通过在关键路径上避免系统调用来降低分配成本。但在内部管理所有这些内存并非易事——分配器需要追踪哪些内存正在使用、哪些空闲，并且要在不成为瓶颈的前提下完成这一切。让我们看看它是如何实现的。</p>
<h2 id="竞技场与内存页">竞技场与内存页</h2>
<p>​	我们提到运行时会向操作系统申请大块内存。这些内存块被称为(Arenas)竞技场，在大多数 64 位系统中每个竞技场为 64MB（Windows 和 32 位系统上为 4MB，WebAssembly 上为 512KB）。</p>
<p>​	当你的程序启动并开始分配内存时，运行时会向操作系统申请第一个竞技场。随着程序需要更多内存，它会申请额外的竞技场。这些竞技场在内存中无需相邻——运行时会通过内部映射表来追踪所有竞技场。</p>
<blockquote>
<p>这是否意味着 Go 会立即占用 64MB 内存？并非如此。当运行时&quot;请求&quot;一个内存区域时，它首先只是预留 64MB 的地址空间——可以理解为在土地上登记了你的名字，但尚未建造任何东西。此时并未使用物理内存。随后，当运行时真正需要使用该内存区域的某些部分时，它会通知操作系统以约 4MB 的块为单位使这些区域可用。即便如此，操作系统也要等到程序实际向这些地址写入数据时才会分配真实内存——物理内存是按需出现的，每次一个操作系统页面，整个过程完全透明。因此实际成本是渐进的：预留空间（基本免费），按需以 4MB 块为单位提交（每次一个系统调用），然后由操作系统在后台根据使用情况填充物理内存。这也是分配器如此快速的另一个原因——一旦内存被提交，分配器的所有操作都无需再与操作系统交互。</p></blockquote>
<p>​	但 64MB 的区块直接分配实在太大了。如果你的程序只申请 32 字节，你肯定不想给它整个竞技场。因此每个竞技场会被划分为 8KB（8192 字节）的页面。这是 Go 语言自定义的页面大小——不同于操作系统通常使用的 4KB 页面。</p>
<p>​	页面是分配器内部运作的基本单位。当需要满足内存分配请求时，分配器以页面为单位进行操作——需要获取多少页面、哪些页面空闲、哪些正在使用。一个 64MB 的竞技场包含 8192 个页面（64MB/8KB），运行时系统会追踪每个页面的状态。</p>
<p>​	但对大多数内存分配来说，8KB 仍然太大了。为一个 32 字节的结构体分配整个页面显然不合理。这时就需要引入跨度（span）的概念。</p>
<h2 id="跨度spans对象的栖身之所">跨度(Spans)：对象的栖身之所</h2>
<p>​	跨度是指一个或多个连续的页面，专门用于存放单一尺寸的对象。这是分配器实际向程序提供内存的层级。</p>
<p>​	让我们具体说明一下。假设你的程序需要大量 32 字节的对象。内存分配器会取一个页面（8KB），将其转换为用于 32 字节对象的跨度，并将其划分为 256 个槽位（8192 / 32 = 256）。每个槽位恰好可以容纳一个对象。当你分配一个 32 字节的对象时，分配器只需找到该跨度中的下一个空闲槽位并返回它。当你需要另一个对象时，它会获取下一个空闲槽位。快速而简单。</p>
<p>​	这是因为每个跨度（span）中的槽位大小都相同。无需寻找合适的块，跨度内没有碎片化，也不需要合并相邻的空闲块。只需找到一个空闲槽位并使用它即可。为了找到空闲槽位，每个跨度都维护一个名为 <code>allocBits</code> 的位图——每个槽位对应一个比特位，其中 1 表示“正在使用”，0 表示“空闲”。寻找下一个空闲槽位只需扫描下一个为 0 的比特位。跨度还记录其在内存中的起始位置、覆盖的页数、槽位总数以及当前已分配的槽位数。此外，还有一个名为 <code>gcmarkBits</code> 的第二位图供垃圾收集器使用——这一点我们稍后会讨论。所有这些元数据都是跨度结构本身的一部分——即运行时分配用于管理跨度的独立对象——并不存储在存放对象的页面内部。因此，一个页面（或跨度覆盖的任意多个页面）的完整 8KB 空间都可用于对象槽位。</p>
<h3 id="大小类别">大小类别</h3>
<p>​	现在，如果每个跨度只容纳一种大小的对象，我们就需要为不同大小准备不同的跨度。但分配器不可能为每个可能的字节数都创建一个跨度——那样会变得难以管理。因此，Go 定义了 68 个大小类别，范围从 8 字节到 32KB。当你分配内存时，比如 20 字节，Go 会将其向上取整到最接近的大小类别（本例中为 24 字节），并使用该类别对应的跨度。虽然取整会损失几个字节，但换来的简洁性和速度是值得的。</p>
<p>​	以下是一些示例：</p>
<table>
  <thead>
      <tr>
          <th style="text-align: left">Class 类别</th>
          <th style="text-align: left">Object Size 对象大小</th>
          <th style="text-align: left">Span Size (Pages) 跨度大小（页数）</th>
          <th style="text-align: left">Objects per Span 每个跨度的对象数</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td style="text-align: left">1</td>
          <td style="text-align: left">8 B(字节)</td>
          <td style="text-align: left">8 KB (1 page)</td>
          <td style="text-align: left">1024</td>
      </tr>
      <tr>
          <td style="text-align: left">4</td>
          <td style="text-align: left">32 B (字节)</td>
          <td style="text-align: left">8 KB (1 page)</td>
          <td style="text-align: left">256</td>
      </tr>
      <tr>
          <td style="text-align: left">10</td>
          <td style="text-align: left">128 B (字节)</td>
          <td style="text-align: left">8 KB (1 page)</td>
          <td style="text-align: left">64</td>
      </tr>
      <tr>
          <td style="text-align: left">32</td>
          <td style="text-align: left">1024 B(字节)</td>
          <td style="text-align: left">8 KB (1 page)</td>
          <td style="text-align: left">8</td>
      </tr>
      <tr>
          <td style="text-align: left">41</td>
          <td style="text-align: left">3072 B(字节)</td>
          <td style="text-align: left">24 KB (3 pages)</td>
          <td style="text-align: left">8</td>
      </tr>
      <tr>
          <td style="text-align: left">46</td>
          <td style="text-align: left">5376 B (字节)</td>
          <td style="text-align: left">16 KB (2 pages)</td>
          <td style="text-align: left">3</td>
      </tr>
      <tr>
          <td style="text-align: left">51</td>
          <td style="text-align: left">8192 B (字节)</td>
          <td style="text-align: left">8 KB (1 page)</td>
          <td style="text-align: left">1</td>
      </tr>
      <tr>
          <td style="text-align: left">60</td>
          <td style="text-align: left">18432 B (字节)</td>
          <td style="text-align: left">72 KB (9 pages)</td>
          <td style="text-align: left">4</td>
      </tr>
      <tr>
          <td style="text-align: left">65</td>
          <td style="text-align: left">27264 B (字节)</td>
          <td style="text-align: left">80 KB (10 pages)</td>
          <td style="text-align: left">3</td>
      </tr>
      <tr>
          <td style="text-align: left">67</td>
          <td style="text-align: left">32768 B (字节)</td>
          <td style="text-align: left">32 KB (4 pages)</td>
          <td style="text-align: left">1</td>
      </tr>
  </tbody>
</table>
<p>​	观察表格时，页面数量可能显得有点随机——为什么 18KB 的对象需要 9 页，而 8KB 的对象只需要 1 页？规则其实很简单：从 1 页开始，持续增加页面，直到末尾的浪费空间（无法容纳另一个对象的剩余字节）小于跨度的 12.5%。对于像 32 字节这样的小对象，一页能容纳 256 个对象且无剩余空间——完美，无需更多页面。对于中等大小的对象，如 3KB 或 5KB，单页会在末尾留下太多不可用空间，因此跨度会增加到 2、3 甚至最多 10 页，以减少浪费。</p>
<p>​	你可能会注意到，有些类别每个跨度只容纳一个对象——比如类别 51（8KB）或类别 67（32KB）。这意味着在单次分配后，跨度就满了。难道使用更多页面让跨度容纳更多对象不是更好吗？不一定。更大的跨度意味着即使你只需要一两个该大小的对象，也会有更多内存被预留。对于像 32 字节这样的小对象，程序通常一次分配数百个，将 256 个打包到一个跨度中是合理的。但对于更大的对象，大多数程序只需要几个，因此保持跨度较小可以避免浪费内存。</p>
<p>​	因此，大小类别系统处理的是 8 字节到 32KB 的常见范围。但并非所有内容都能完美地适应这个范围。</p>
<h3 id="边缘情况大对象与小对象">边缘情况：大对象与小对象</h3>
<p>​	你可能已经注意到一个奇怪的地方：我们说有 68 个大小类别，但表格只从 1 到 67——只有 67 个。缺失的那个在哪里？那就是大小类别 0，专为大于 32KB 的对象保留。与其他类别不同，它没有固定的对象大小或跨度大小。相反，类别 0 的跨度会精确分配对象所需的页面数量，不进行槽位细分——一个对象对应一个跨度。</p>
<p>​	另一方面，像 <code>bool</code> 或 <code>int8</code> 这样极小的对象会遇到另一个问题。最小的大小类别是 8 字节，所以即使是 1 字节的值也会占用一个 8 字节的槽位——对于如此小的对象来说浪费很大。为了解决这个问题，Go 有一个微型分配器，它将多个微型对象（小于 16 字节且不包含指针）打包到一个 16 字节的块中。这样，多个布尔值或小整数可以共享一个槽位，而不是各自占用一个。这极大地减少了分配大量小值时的浪费。</p>
<p>​	我们已经了解了跨度是如何按大小组织的——但大小并不是分配器唯一关心的事情。</p>
<h3 id="跨度spans类别大小--指针">跨度(Spans)类别：大小 + 指针</h3>
<p>​	内存分配器不仅关心对象的大小，还关心对象是否包含指针。为什么？因为垃圾回收器需要扫描包含指针的对象，以便追踪引用并找到存活数据。不包含指针的对象（比如一个 <code>[100]byte</code> 或仅包含整数的结构体）在垃圾回收期间可以安全跳过——没有需要追踪的内容。</p>
<p>​	因此，Go 为每种情况维护独立的跨度：一种用于需要扫描的对象（scan），另一种用于不需要扫描的对象（noscan）。大小类别与扫描/非扫描标志的组合被称为跨度类别。由于有 68 种大小类别，每种又有 2 种变体，总共就有 136 种跨度类别。</p>
<p>​	综合以上，完整的图景如下所示：</p>
<p><img loading="lazy" src="https://cdn.jsdelivr.net/gh/Lucareful/RepoImg/blog/20260310174420478.webp" alt="Arena, pages, spans and objects layout"  />
</p>
<p>​	分配器向操作系统请求内存区域，将其划分为页面，将页面分组为跨度，再将跨度划分为固定大小的槽位。每一级都使下一级变得易于管理。但我们还有一个尚未讨论的大问题：Go 程序同时运行许多 goroutine，它们都需要分配内存。如何在不使分配器成为瓶颈的情况下，保持这一切的有序运行？</p>
<h2 id="锁竞争问题">锁竞争问题</h2>
<p>​	至此我们已经了解了内存的组织方式——竞技场、页面、跨度、槽位。但还有一个关键问题尚未解决：当多个协程同时尝试分配内存时会发生什么？</p>
<p>​	假设你只有一个全局跨度列表。每次任何协程需要内存时，都必须先获取锁，找到空闲槽位，然后释放锁。当数千个协程并发运行时，这把锁就会成为瓶颈——协程花费在相互等待上的时间，比实际执行有效工作的时间还要多。</p>
<p>​	这就是锁竞争问题，而解决这个问题正是 Go 分配器设计中最重要的环节之一。解决方案采用三级层次结构，每一级都有不同的作用域和锁行为：</p>
<h3 id="第一级mcache每个-p-独享无需加锁"><strong>第一级：mcache（每个 P 独享，无需加锁）</strong></h3>
<p>​	还记得在引导程序文章中提到的，Go 调度器拥有固定数量的 P（处理器），通常每个 CPU 核心对应一个。每个 P 都有自己专属的 mcache——这是一组私有的内存跨度集合，每个跨度类别对应一个跨度。当运行在某个 P 上的 goroutine 需要分配内存时，它会从该 P 的 mcache 中获取一个槽位。由于同一时间只有一个 goroutine 在 P 上运行，因此无需加锁。这是快速路径，处理了绝大多数内存分配请求。</p>
<h3 id="第二级mcentral按跨度类别划分短暂加锁"><strong>第二级：mcentral（按跨度类别划分，短暂加锁）</strong></h3>
<p>​	当 mcache 中某个特定跨度类别的跨度已满时，就需要获取新的跨度。这时就轮到 mcentral 发挥作用了。136 个跨度类别各自对应一个 mcentral，每个 mcentral 维护着共享的跨度池。mcache 会将已满的跨度归还给 mcentral，并从中获取一个含有空闲槽位的新跨度。这个过程需要加锁，但锁持有时间很短——仅仅是交换两个跨度。由于每个跨度类别都有独立的 mcentral，分配不同大小内存或扫描/非扫描变体的 goroutine 之间不会相互竞争。</p>
<h3 id="第三级mheap全局昂贵的锁"><strong>第三级：mheap（全局，昂贵的锁）</strong></h3>
<p>​	当一个 mcentral 没有更多 span 可以分配时，它会向 mheap 请求新的页面来创建一个新的 span。mheap 是全局的页面分配器——只有一个，访问它需要一个全局锁。这是慢速路径。它涉及搜索空闲页面，可能向操作系统请求一个新的 arena，并初始化一个新的 span。但这种情况很少发生，因为上面的层级吸收了大部分的需求。</p>
<p>​	这也是我们之前提到的大对象（&gt;32KB）最终的去处——它们跳过 mcache 和 mcentral，直接进入 mheap。</p>
<p>整个设计就像一个缓存链：</p>
<p><img loading="lazy" src="https://cdn.jsdelivr.net/gh/Lucareful/RepoImg/blog/20260310231232488.webp" alt="Memory allocator hierarchy levels"  />
</p>
<p>​	每一层都充当下一层的缓存。快速路径是无锁的，中等路径使用细粒度锁，而慢速路径在实际中足够罕见，其开销可以忽略不计。这一设计基于名为 tcmalloc（线程缓存分配器）的方法，最初由谷歌为 C/C++程序设计，但已针对 Go 的特定需求进行了调整。</p>
<p>​	既然我们已经理解了结构和层级，接下来让我们一步步了解实际发生的过程。</p>
<h2 id="分配流程">分配流程</h2>
<p>​	所有操作都通过运行时中的一个单一函数完成：位于 <code>src/runtime/malloc.go</code> 中的 <code>mallocgc()</code> 。</p>
<p>​	它首先检查分配的大小。根据对象的大小，会采取截然不同的路径。让我们从最简单的情况开始。</p>
<h3 id="零大小分配">零大小分配</h3>
<p>​	一个有趣的边界情况：如果你分配一个零大小的对象，比如 <code>var x struct{}</code> 会怎样？Go 不会费心分配任何内存——所有零字节的分配都会返回指向同一个全局变量（<code>zerobase</code>）的指针。这是安全的，因为你永远无法真正通过零大小对象进行读取或写入。</p>
<p>​	现在来看真正的分配，从最小的开始。</p>
<h3 id="微小对象">微小对象</h3>
<p>​	对于不含指针的微小对象——比如 <code>bool</code> 、 <code>int8</code> ，或者不含指针的小型结构体——分配器会使用我们之前提到的微小分配器。mcache 会记录当前的微小块（其实就是从大小类别 2 的 span 中取出的普通 16 字节槽位——它本身没什么特别之处）以及一个偏移量，该偏移量标记了该块目前已被使用了多少。</p>
<p>​	当需要进行微小分配时，分配器首先会将大小向上取整以进行适当的对齐（根据对象大小取整为 2、4 或 8 字节），然后检查当前的微小块从当前偏移量到末尾是否有足够的空间。如果空间足够，分配器会返回一个指向 <code>block + offset</code> 的指针，并推进偏移量。因此，一个 1 字节的 <code>bool</code> 后面跟着一个 1 字节的 <code>int8</code> ，它们会被打包在同一个 16 字节块内相邻的位置。</p>
<p>​	当当前的小块内存没有足够空间时，分配器会从 mcache 的 2 号尺寸类跨度中获取一个新的 16 字节槽位（使用常规小对象路径），并将对象放置在其起始位置。但这里有个微妙细节：分配器不会盲目切换到新块。它会比较旧块剩余的空闲空间与新块剩余的空间（即 16 减去刚放置的对象大小）。剩余空间更多的块将成为当前的小块内存。这种做法能最大限度地减少浪费——分配器总是倾向于为未来的微小分配保留最多空间的块。无论哪种情况，你所请求的对象都会从新槽位返回；这只是一个关于哪个块在下次微小分配时保持&quot;当前&quot;状态的问题。</p>
<p>​	由于微小分配器会捕获所有 16 字节以下的无指针对象，它最终会吸收你预期会落入最小尺寸类的大部分对象。实际上，8 字节尺寸类（类 1）专门用于确实包含指针的 8 字节值——比如 <code>*int</code> 或单指针接口。而 <code>int64</code> 尽管也是 8 字节，却会通过微小分配器处理。</p>
<p>​	当对象达到 16 字节或更大（或包含指针）时，微分配器不再适用，我们将进入主分配路径。</p>
<h3 id="小型对象16-字节至-32kb">小型对象（16 字节至 32KB）</h3>
<p>​	这是最常见的分配路径，也是整个层级结构优化的核心。其流程如下：</p>
<ol>
<li>将所需大小向上取整至最接近的尺寸类别，并确定跨度类别（尺寸 + 扫描/非扫描属性）。</li>
<li>检查线程缓存：查找对应跨度类别的跨度，并通过位图定位下一个空闲槽位。若存在空闲槽位，则直接返回该内存块——整个过程无需加锁，仅涉及位运算操作。</li>
<li>若该跨度已满，线程缓存会将其归还至中心缓存，并请求获取含空闲槽位的新跨度。中心缓存首先在已有跨度中查找可用资源。若发现尚未被垃圾回收器清扫的跨度，会先执行清扫操作，再将其移交给线程缓存。</li>
<li>如果 mcentral 没有可用内存，它会向 mheap 请求分配新页面并创建新的 span。</li>
<li>如果 mheap 没有足够的空闲页面，它会向操作系统请求一个新的 arena。</li>
</ol>
<p>大多数分配在步骤 2 就停止了。步骤 3-5 发生的频率逐渐降低，这就是系统性能良好的原因。</p>
<h3 id="大对象-32kb">大对象（&gt; 32KB）</h3>
<p>​	正如我们之前所介绍的，这些对象完全跳过 mcache 和 mcentral，直接进入 mheap，后者会精确分配所需的页面。</p>
<p>​	我们已经了解了内存是如何分配的，但反方向呢——内存又是如何被释放的？</p>
<h2 id="垃圾回收集成">垃圾回收集成</h2>
<p>​	内存分配器并非独立运作——它与垃圾回收器紧密相连。</p>
<p>​	垃圾收集器的任务是判断堆上哪些对象仍在被使用，哪些是垃圾。它通过遍历对象图来实现这一点——从已知的根（全局变量、栈变量等）开始，沿着指针追踪所有可达的对象。任何无法到达的对象都是死对象，可以被释放。</p>
<p>​	这就是每个跨度中两个位图发挥作用的地方。每个跨度都有一个 <code>allocBits</code> 位图（记录哪些槽位已分配）和一个 <code>gcmarkBits</code> 位图（记录垃圾收集器发现哪些槽位是存活的）。在垃圾收集周期中，收集器在 <code>gcmarkBits</code> 中标记存活对象。标记完成后，运行时会交换这两个位图——这样 <code>allocBits</code> 现在仅反映存活对象，所有未被标记的对象实际上就被释放了。分配器随后可以重用这些槽位。</p>
<p>​	这也解释了你在分配流程中可能注意到的一个现象：当 mcentral 将 span 交给 mcache 时，有时需要先进行扫描。扫描是指通过检查 span 的位图，确定在垃圾回收周期后哪些槽位处于空闲状态的过程。分配器采用惰性扫描策略——它不会一次性扫描所有 span，而是在需要为新分配提供空间时按需扫描。这种方式将扫描的开销分散到所有分配操作中，避免了集中扫描导致的长时间停顿。</p>
<p>​	如果某个 span 在扫描后完全变空（所有对象都成了垃圾），其占用的页面将返还给 mheap，并可用于其他 span 类别。</p>
<p>​	因此，垃圾回收器负责识别死亡对象，而分配器则回收这些对象占用的槽位。但还有一个问题：这些内存是否会返还给操作系统？</p>
<h2 id="内存释放与回收机制">内存释放与回收机制</h2>
<p>​	当垃圾回收器释放对象时，它们并不会归还给操作系统。这些内存槽位只是在其所属的跨度中重新变为可用状态，等待下一次分配。从操作系统的视角来看，这些页面始终由运行时系统持有——你的程序仍然占用着所有这些内存。</p>
<p>​	但如果你的程序曾出现活动高峰，分配了大量内存，而其中大部分现已变成垃圾呢？此时会有大量空闲页面闲置在内存堆中无所作为，而操作系统却认为你的程序仍在占用所有这些内存。</p>
<p>​	这时就需要清扫器登场了。它是一个后台协程，会定期查找长期未使用的空闲页面，并通知操作系统可以回收这些内存。这些页面仍会映射在你程序的地址空间中（这样运行时系统后续无需系统调用即可复用它们），但操作系统知道可以收回其背后的物理内存。在 Linux 系统中，这是通过 <code>MADV_DONTNEED</code> 实现的——这个提示相当于告诉系统“我暂时不需要这些内存，请随意用于其他地方。”</p>
<p>​	这是一种平衡的艺术。过早归还内存会损害性能——如果程序很快又需要那块内存，就得重新触发缺页中断。但保留过多未使用的内存又会浪费系统资源。清理器试图找到恰当的平衡点。</p>
<h2 id="小结">小结</h2>
<p>​	让我们回顾一下已讨论的内容。在编译时，逃逸分析决定哪些值需要分配在堆上。在运行时，内存分配器负责实际管理堆内存。运行时不会每次都向操作系统申请内存，而是预先获取 64MB 的大块内存区域，并将其细分为 8KB 的页面。页面被分组为跨度，每个跨度包含固定大小的槽位，用于存放单一尺寸的对象——共有 68 种尺寸类别，范围从 8 字节到 32KB。扫描/非扫描的区分使跨度类别翻倍至 136 种，这样垃圾回收器就可以跳过不含指针的对象。</p>
<p>​	为了避免锁竞争，内存分配器采用三级层级结构：mcache（每个 P 独享，无锁）处理大多数分配请求，mcentral（按跨度类别划分，使用短暂锁）为 mcache 补充新的内存跨度，而 mheap（全局）在其他层级耗尽时分配页面。微小对象会被打包存放，大型对象则完全绕过此层级结构。</p>
<p>​	分配器通过每个内存跨度上的双重位图与垃圾回收器协同工作，而内存清理器确保未使用的内存最终归还给操作系统。</p>
<p>​	如果你想亲自探索实现细节，运行时源码中的 <code>src/runtime/malloc.go</code> 、 <code>mheap.go</code> 、 <code>mcache.go</code> 和 <code>mcentral.go</code> 文件注释详尽且出奇地易于阅读。</p>
]]></content:encoded>
    </item>
    <item>
      <title>转载 | 理解 Go 运行时（一）：引导程序</title>
      <link>https://luenci.com/en/posts/%E7%90%86%E8%A7%A3-go-%E8%BF%90%E8%A1%8C%E6%97%B6%E4%B8%80%E5%BC%95%E5%AF%BC%E7%A8%8B%E5%BA%8F/</link>
      <pubDate>Fri, 27 Feb 2026 00:00:00 +0000</pubDate>
      <guid>https://luenci.com/en/posts/%E7%90%86%E8%A7%A3-go-%E8%BF%90%E8%A1%8C%E6%97%B6%E4%B8%80%E5%BC%95%E5%AF%BC%E7%A8%8B%E5%BA%8F/</guid>
      <description>&lt;blockquote&gt;
&lt;p&gt;参考文章：https://internals-for-interns.com/posts/understanding-go-runtime&lt;/p&gt;
&lt;p&gt;本次程序实验环境均在 ： mac os arm64  架构上运行&lt;/p&gt;</description>
      <content:encoded><![CDATA[<blockquote>
<p>参考文章：https://internals-for-interns.com/posts/understanding-go-runtime</p>
<p>本次程序实验环境均在 ： mac os arm64  架构上运行</p></blockquote>
<h1 id="tldrai总结人工审核">TL,DR（AI总结，人工审核）</h1>
<p>​	文章介绍 Go 程序启动时 runtime 的引导过程：从创建首个线程与 goroutine、初始化 TLS、CPU 检测，到调度器、内存分配器、GC 与 P 结构建立，最终启动 system monitor 并执行包初始化，才进入 main。</p>
<h2 id="前言">前言</h2>
<p>​	当你编写 Go 代码时，幕后会发生许多事情。协程是轻量级的，通道能直接运行，内存会自动管理，你完全无需考虑线程池。这一切都由 Go 运行时驱动——这是一个复杂的底层架构，会被编译进每个 Go 二进制文件中。</p>
<p>​	但在这些机制开始工作之前，必须完成初始化。这就是引导程序的作用——它是操作系统启动二进制文件后，到你的 <code>func main()</code> 获得控制权之前运行的进程。</p>
<h2 id="go-语言执行空操作的速度有多快">Go 语言执行空操作的速度有多快？</h2>
<p>这是一个完全不做任何事情的 C 程序：</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">3
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-c" data-lang="c"><span style="display:flex;"><span><span style="color:#ff7b72">int</span> <span style="color:#d2a8ff;font-weight:bold">main</span>() {
</span></span><span style="display:flex;"><span>    <span style="color:#ff7b72">return</span> <span style="color:#a5d6ff">0</span>;
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></td></tr></table>
</div>
</div><p>这是等价的 Go 语言版本：</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">3
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">4
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#ff7b72">package</span> main
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ff7b72">func</span> <span style="color:#d2a8ff;font-weight:bold">main</span>() {
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></td></tr></table>
</div>
</div><p>等价的 python 语言版本（个人探究对比）：</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">3
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">4
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">5
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#ff7b72">def</span> <span style="color:#d2a8ff;font-weight:bold">main</span>():
</span></span><span style="display:flex;"><span>    <span style="color:#ff7b72">return</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#ff7b72">if</span> __name__ <span style="color:#ff7b72;font-weight:bold">==</span> <span style="color:#a5d6ff">&#34;__main__&#34;</span>:
</span></span><span style="display:flex;"><span>    main()
</span></span></code></pre></td></tr></table>
</div>
</div><p>三者编译后对比:</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 2
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 3
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 4
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 5
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 6
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 7
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 8
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679"> 9
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">10
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">11
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">12
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">13
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">14
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>$ gcc -o nothing_c nothing.c
</span></span><span style="display:flex;"><span>$ go build -o nothing_go nothing.go
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>$ ls -lh nothing_c nothing_go dist/nothing
</span></span><span style="display:flex;"><span>-rwxr-xr-x@ <span style="color:#a5d6ff">1</span> luenci  staff   6.8M  2月 <span style="color:#a5d6ff">26</span> 11:04 dist/nothing
</span></span><span style="display:flex;"><span>-rwxr-xr-x@ <span style="color:#a5d6ff">1</span> luenci  staff    16K  2月 <span style="color:#a5d6ff">26</span> 10:44 nothing_c
</span></span><span style="display:flex;"><span>-rwxr-xr-x@ <span style="color:#a5d6ff">1</span> luenci  staff   1.7M  2月 <span style="color:#a5d6ff">26</span> 10:44 nothing_go
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>$ time ./nothing_c
</span></span><span style="display:flex;"><span>real    0m0.001s
</span></span><span style="display:flex;"><span>$ time ./nothing_go
</span></span><span style="display:flex;"><span>real    0m0.002s
</span></span><span style="display:flex;"><span>$ time dist/nothing
</span></span><span style="display:flex;"><span>real	  0.11s
</span></span></code></pre></td></tr></table>
</div>
</div><blockquote>
<p>虽然编译型语言和解释性语言有天然性能只查，但 python 确实很遥遥落后，当然也有一些技术手段来对 python 进行编译优化，见下表</p></blockquote>
<table>
  <thead>
      <tr>
          <th><strong>工具</strong></th>
          <th><strong>原理</strong></th>
          <th><strong>易用性</strong></th>
          <th><strong>代码安全性</strong></th>
          <th><strong>运行速度</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>PyInstaller</strong></td>
          <td>捆绑打包</td>
          <td>⭐⭐⭐⭐⭐</td>
          <td>低 (易被反编译)</td>
          <td>正常</td>
      </tr>
      <tr>
          <td><strong>Nuitka</strong></td>
          <td>转译为 C++</td>
          <td>⭐⭐⭐</td>
          <td>高</td>
          <td>较快</td>
      </tr>
      <tr>
          <td><strong>PyOxidizer</strong></td>
          <td>内存加载/Rust 构建</td>
          <td>⭐⭐</td>
          <td>极高</td>
          <td>快</td>
      </tr>
  </tbody>
</table>
<p>回到正题，Go 语言编译出的二进制文件几乎大了 100 倍，运行时间也大约长了两倍。而我们什么都没做。这是怎么回事？</p>
<p>​	答案是，Go 语言在你的 <code>main</code> 函数运行之前做了大量工作。那额外的 1.5MB 二进制文件包含了完整的运行时系统：内存分配器、垃圾回收器、调度器、系统监控器，以及支持协程、通道和映射所需的所有机制。在你获得控制权之前，Go 必须完成所有这些设置。</p>
<h2 id="引导概览">引导概览</h2>
<p>让我们逐步了解整个引导过程——从操作系统启动你的二进制文件，到你的 <code>func main()</code> 函数最终运行之间发生的一切。</p>
<p>以下是全局概览——运行时系统在你的代码运行前采取的每一步：</p>
<p><img loading="lazy" src="https://cdn.jsdelivr.net/gh/Lucareful/RepoImg/blog/20260226132531697.png" alt="Go bootstrap big picture"  />
</p>
<h2 id="入口点并非你的-main函数">入口点：并非你的 main()函数</h2>
<p>第一个令人惊讶的事实：你的 <code>main</code> 函数并非程序的入口点。我们可以证明这一点。让我们使用 <code>readelf</code> 来找出我们那个什么都不做的二进制文件的实际入口点：</p>
<blockquote>
<p>也可以自己将完整的 nm 堆栈信息输出到文件中查看，main.main 函数并不是在最上面！</p>
<p>❯  go tool nm nothing_linux &gt; a.txt</p></blockquote>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">2
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>$ readelf -h nothing_go | grep <span style="color:#a5d6ff">&#34;Entry point&#34;</span>
</span></span><span style="display:flex;"><span>  Entry point address:               0x4787a0
</span></span></code></pre></td></tr></table>
</div>
</div><p>那是一个原始地址。那里存放的是什么函数呢？ <code>go tool nm</code> 可以将地址映射到符号名称：</p>
<div class="highlight"><div style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">
<table style="border-spacing:0;padding:0;margin:0;border:0;"><tr><td style="vertical-align:top;padding:0;margin:0;border:0;">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">1
</span><span style="white-space:pre;-webkit-user-select:none;user-select:none;margin-right:0.4em;padding:0 0.4em 0 0.4em;color:#737679">2
</span></code></pre></td>
<td style="vertical-align:top;padding:0;margin:0;border:0;;width:100%">
<pre tabindex="0" style="color:#e6edf3;background-color:#0d1117;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>$ go tool nm nothing_linux | grep 4787a0
</span></span><span style="display:flex;"><span> 4787a0 T _rt0_amd64_linux
</span></span></code></pre></td></tr></table>
</div>
</div><p>​	这就是入口点—— <code>_rt0_amd64_linux</code> ，而非 <code>main.main</code> 。实际的入口点位于运行时内部的一个汇编函数中（在 <code>src/runtime/rt0_linux_amd64.s</code> ）。Go 支持的每种架构都有对应的入口点： <code>_rt0_arm64_linux</code> 、 <code>_rt0_386_linux</code> 等等。它们所做的就是从栈中获取命令行参数，然后跳转到 <code>rt0_go</code> （位于 <code>src/runtime/asm_amd64.s</code> ），真正的引导程序就从这里开始。这是一个庞大的汇编函数，在 Go 代码运行之前奠定基础。以下是它大致按顺序执行的操作：</p>
<p>​	首先，它创建了 Go 从一开始就需要的两样东西：g0 和 m0。可以这样理解——<strong>Go 在 goroutine 上运行你的代码，而 goroutine 又在操作系统线程上运行</strong>。因此，在一切开始之前，运行时至少需要各一个。这就是 g0 和 m0：第一个 goroutine 和第一个线程。不过 g0 有点特殊——它不会运行你的代码。它被保留用于运行时自身的内部管理，比如调度其他 goroutine。</p>
<p>​	接着它会设置线程本地存储（TLS）。TLS 是一种操作系统级别的机制，为每个线程提供独立的私有存储区域——不同线程读取同一个 TLS 槽位会获得不同的值。Go 语言利用 TLS 存储指向当前线程上运行协程的指针，这样运行时系统就能快速且无需加锁地回答“我现在是哪个协程？”这个关键问题。其重要性使得运行时系统会立即通过写入特定校验值并读回进行测试——若 TLS 无法正常工作，程序将立即中止。</p>
<p>​	它还会检查运行在何种 CPU 上——包括制造商以及支持的功能特性。Go 二进制文件可以编译以利用更新的 CPU 指令来提升性能，因此运行时会验证 CPU 是否确实具备这些功能。如果 CPU 不支持，程序会输出错误信息并退出，而不是在后续执行中因非法指令而崩溃。</p>
<p>如果二进制文件是使用 CGO 支持构建的，那么在继续之前需要额外初始化 C 运行时的步骤。</p>
<p>​	所有汇编层面的基础工作完成后——TLS 正常运行、CPU 特性已知、g0 和 m0 已链接—— <code>rt0_go</code> 通过四次函数调用过渡到 Go 代码： <code>check()</code> 验证编译器假设是否正确， <code>args()</code> 保存命令行参数， <code>osinit()</code> 检测 CPU 数量（这将作为默认的 <code>GOMAXPROCS</code> ），最后是 <code>schedinit()</code> ——真正的工作在此处展开。</p>
<h2 id="调度器初始化schedinit">调度器初始化（schedinit）</h2>
<p>现在轮到重头戏了。 <code>schedinit()</code> （位于 <code>src/runtime/proc.go</code> 中）是负责初始化所有关键运行时子系统的主函数。让我们按顺序梳理它的执行流程。</p>
<h3 id="stop-the-world--停止世界">Stop the World （ 停止世界）</h3>
<p>​	<code>schedinit()</code> 首先要做的是将世界标记为停止状态。&ldquo;停止世界&quot;是你在 Go 运行时讨论中经常听到的一个术语——它意味着暂停所有 goroutine，以便运行时能够安全地执行需要同时没有其他任务运行的工作。在这种情况下，还没有任何 goroutine 存在，所以根据定义，世界已经停止了。但运行时明确地标记了这一点，因为几个子系统的行为会根据 goroutine 是否可能并发运行而有所不同。</p>
<h3 id="stack-pool-initialization-栈池初始化">Stack Pool Initialization （栈池初始化）</h3>
<p>​	goroutine 需要栈才能运行。Go 的 goroutine 从微小的 2KB 栈开始，动态增长，运行时按大小组织预分配的栈段池，以便快速创建新的 goroutine。 <code>stackinit()</code> 负责设置这些池。</p>
<p>​	当一个 goroutine 完成执行且其栈被释放时，它会回到池中等待重用，而不是归还给操作系统。这对性能至关重要——goroutine 的创建必须成本低廉，若每次执行 <code>go</code> 语句都向操作系统申请内存，速度将慢得无法接受。</p>
<p>但栈只是 goroutine 使用的内存类型之一。它们还需要堆内存——用于任何逃逸出栈的内容，比如切片、映射或通过指针返回的值。</p>
<h3 id="memory-allocator-initialization-内存分配器初始化">Memory Allocator Initialization （内存分配器初始化）</h3>
<p>​	这就是 <code>mallocinit()</code> 负责处理的部分。它负责设置 Go 的内存分配器，其核心理念相当直观：与其每次代码执行 <code>make([]byte, 100)</code> 时都向操作系统请求内存，Go 会预先获取大块内存，然后从这些大块中分配小块内存。这样速度要快得多。</p>
<p>​	分配器按大小类别组织内存——共有 68 个类别，范围从 8 字节到 32KB。当你分配一个 50 字节的对象时，Go 不会精确地给你 50 字节。它会向上取整到最接近的大小类别（本例中为 64 字节），并从预先划分的 64 字节槽块中分配一个槽位给你。这种做法简化了操作并避免了内存碎片。对于任何大于 32KB 的对象，分配器会完全跳过大小类别，直接从堆中分配内存。</p>
<p>​	真正巧妙的部分在于后续创建 P（处理器）时。每个 P 都拥有自己的本地内存缓存，因此大多数内存分配完全不需要加锁——goroutine 只需从所属 P 的缓存中获取内存。只有当该缓存耗尽时，才需要访问共享的中心列表进行补充。这正是 Go 语言即使在高并发场景下仍能实现极速内存分配的重要原因。</p>
<p>在栈和堆内存这两大核心组件就位后，运行时系统开始处理一些规模较小但至关重要的细节。</p>
<h3 id="cpu-flags-and-hash-initialization-cpu-标志与哈希初始化">CPU Flags and Hash Initialization （CPU 标志与哈希初始化）</h3>
<p><code>cpuinit()</code> 执行比汇编代码已检测内容更详细的 CPU 能力检查——精确确定哪些指令集扩展可用。</p>
<p>​	接着， <code>alginit()</code> 会选取 Go 语言映射表将使用的哈希函数。如果 CPU 支持硬件 AES 指令，Go 会利用它们进行哈希计算——这能显著提升速度。否则，它将回退到软件实现。这一选择会影响程序中每一个映射表操作，因此值得在早期就做出正确决策。</p>
<p>既然运行时已经了解了硬件的性能，现在就该在其之上搭建软件基础设施了。</p>
<h3 id="modules-types-and-the-main-thread-模块类型与主线程">Modules, Types, and the Main Thread （模块、类型与主线程）</h3>
<p>​	运行时在此构建支撑 Go 类型系统的内部表格。 <code>modulesinit()</code> 构建所有已编译包的表格——每个表格都包含类型信息、函数元数据以及 GC 位图。 <code>typelinksinit()</code> 和 <code>itabsinit()</code> 建立实现 Go 接口功能的接口分发表。而 <code>mcommoninit()</code> 则完成 m0（我们的主线程）的设置，并将其注册到全局线程列表中。</p>
<p>内部管道就绪后，运行时终于可以开始向外探索——审视程序从外部世界接收的输入。</p>
<h3 id="args-environment-and-security-参数环境与安全">Args, Environment, and Security （参数、环境与安全）</h3>
<p>​	<code>goargs()</code> 将原始的 C 风格 <code>argv</code> 转换为将成为 <code>os.Args</code> 的 Go 字符串切片。 <code>goenvs()</code> 对环境变量执行相同的操作。接着 <code>secure()</code> 执行安全检查，而 <code>checkfds()</code> 确保标准输入、标准输出和标准错误实际上处于打开状态——防止因关闭的标准文件描述符而可能引发的一类安全问题。</p>
<p>其中一个环境变量值得特别关注。</p>
<h4 id="debug-environment-调试环境变量">Debug Environment （调试环境变量）</h4>
<p><code>GODEBUG</code> 变量控制着各种运行时行为。以下是几个实用的设置：</p>
<ul>
<li><code>GODEBUG=inittrace=1</code> — 打印每个包初始化函数的计时信息</li>
<li><code>GODEBUG=schedtrace=1000</code> — 每秒打印调度器状态</li>
<li><code>GODEBUG=gctrace=1</code> — 打印垃圾回收事件</li>
</ul>
<p>这些内容在此处被解析并应用，以便在后续的引导过程中生效。至此，所有支撑组件都已就位。运行时现在转向两个剩余的主要子系统。</p>
<h3 id="garbage-collector-initialization-垃圾回收器初始化">Garbage Collector Initialization （垃圾回收器初始化）</h3>
<p>​	<code>gcinit()</code> 准备启动 Go 的垃圾回收器——这个系统会自动释放程序不再需要的内存。Go 采用并发标记清除式垃圾回收机制，这意味着它的大部分工作会在程序持续运行的同时进行，而非完全暂停所有程序执行。</p>
<p>​	在初始化过程中，运行时会设置垃圾收集器后续所需的机制：包括决定何时触发回收的步调器（默认在堆内存翻倍时启动）、在回收后回收未使用内存的清扫器，以及供垃圾收集工作线程使用的每个处理器专属工作队列，用于追踪哪些对象仍然存活。</p>
<p>​	但这里有一个重要细节：垃圾回收器已初始化但尚未启用。它实际上要等到稍后在 <code>runtime.main()</code> 中调用 <code>gcenable()</code> 时才会开始运行。为什么呢？因为启用垃圾回收器需要启动 goroutine（用于后台清理和内存回收）并创建通道——而在调度器和运行时的其他部分完全设置好之前，这些操作都无法进行。更重要的是，在类型元数据和指针映射准备就绪前触发垃圾回收周期，可能导致收集器扫描不完整的数据结构。</p>
<h3 id="processor-p-initialization-处理器p初始化">Processor (P) Initialization （处理器（P）初始化）</h3>
<p>​	运行时需要创建 P（处理器）结构。可以将 P 视为一个工作站：goroutine 需要坐在上面才能完成任何工作，而操作系统线程则是操作它的工人。每个 P 都自带等待运行的 goroutine 队列、自己的内存缓存（因此分配速度快且无需锁）、以及自己的定时器和垃圾回收工作器状态。</p>
<p>​	P 的数量由 <code>GOMAXPROCS</code> 决定，默认值为之前检测到的 CPU 核心数。因此，在 8 核机器上，你将获得 8 个 P——这意味着在任何给定时刻，最多可以有 8 个 goroutine 真正并行运行。</p>
<h3 id="start-the-world-启动世界">Start the World （启动世界)</h3>
<p>​	至此， <code>schedinit()</code> 调用了 <code>worldStarted()</code> 。“世界”现在被视为已启动——所有基础设施都已就位，goroutine 可以并发运行。餐厅正式开门营业。随着调度器、分配器、垃圾回收器以及所有支持基础设施就位，运行时终于准备好创建它的第一个真正的 goroutine。</p>
<h2 id="creating-the-main-goroutine-创建主-goroutine">Creating the Main Goroutine (创建主 Goroutine)</h2>
<p>​	可以把到目前为止的一切想象成造车——我们已经组装好了发动机（调度器）、燃油系统（内存分配器）和排气系统（垃圾回收器）。现在该转动钥匙启动了。</p>
<p>​	回到 <code>rt0_go</code> ，在 <code>schedinit()</code> 返回后，运行时创建了它的第一个 goroutine。但请注意——这个 goroutine 并不运行你的 <code>main.main</code> 。它运行的是 <code>runtime.main</code> ，这是运行时自己的主函数。你的代码稍后才会执行。</p>
<p>​	该协程获得一个 2KB 的初始栈（来自我们之前设置的栈池），并被放置到第一个 P 的运行队列中，准备就绪。随后运行时在 m0 上启动调度循环——这是关键的转折点。调度器立即选取该协程并开始执行 <code>runtime.main()</code> 。</p>
<p>​	引擎正在运行。我们即将抵达你的代码——但还差一点。</p>
<h2 id="runtimemain-最后一英里">runtime.main (最后一英里)</h2>
<p>​	我们终于进入了以适当 goroutine 运行的 Go 代码。但在你的代码运行之前，仍有工作要做。以下是 <code>runtime.main()</code> （位于 <code>src/runtime/proc.go</code> 中）：</p>
<h3 id="max-stack-size-and-system-monitor-最大栈大小与系统监控器">Max Stack Size and System Monitor (最大栈大小与系统监控器)</h3>
<p>​	首先， <code>runtime.main()</code> 对任何 goroutine 栈的最大增长设置了限制——在 64 位系统上为 1GB。如果一个 goroutine 超出了这个限制（通常是由于无限递归），程序会因栈溢出而 panic。</p>
<p>​	接着启动系统监控器（sysmon）——这是一个专用的后台线程，充当运行时的看门狗。它独立于调度器运行，负责监控各种情况：如果某个 goroutine 占用处理器（P）时间过长，sysmon 会强制其让出；如果操作系统线程在系统调用中卡住，sysmon 会收回其处理器并分配给其他线程，以便其他 goroutine 能继续运行。它还会在垃圾回收（GC）长时间未运行时触发回收，检查就绪的网络 I/O，并将未使用的内存返还给操作系统。</p>
<p>​	主 goroutine 此时也会被锁定到主操作系统线程上。这是为了兼容某些 C 语言库和 GUI 框架，这些库和框架期望特定操作始终在“主”线程上执行。</p>
<p>看门狗已启动，主线程也已就绪，运行时现在可以开始运行你的代码了——嗯，差不多吧。还有一些事情需要处理。</p>
<h3 id="runtime-init-functions-运行时初始化函数">Runtime init() Functions (运行时初始化函数)</h3>
<p>​	首先，运行时运行其内部的 <code>init()</code> 函数——这些函数属于 <code>runtime</code> 包及其依赖项。这些函数完成在 <code>schedinit()</code> 期间尚未完全准备好的内部数据结构的设置。</p>
<p>​	运行时自身的初始化完成后，调度器已全面就绪，类型元数据准备就绪，通道功能正常。这意味着终于可以安全地启用垃圾回收器了。</p>
<h3 id="enabling-the-garbage-collector-启用垃圾回收器">Enabling the Garbage Collector (启用垃圾回收器)</h3>
<p>​	还记得我们说过 GC 已初始化但未启用吗？现在它终于被激活了。 <code>gcenable()</code> 会启动后台清扫器和回收器的 goroutine，从此刻起，GC 将在后台运行，每当堆内存增长到足够大时，就会自动回收未使用的内存。</p>
<p>为何现在启用而非稍后？因为下一步——运行包的初始化函数——可能会分配大量内存。届时垃圾回收器必须处于活跃状态。</p>
<h3 id="running-package-init-functions-运行包初始化函数">Running Package init() Functions (运行包初始化函数)</h3>
<p>​	现在来谈谈你可能熟悉的内容： <code>init()</code> 函数。运行时系统会遍历程序中的每个包，并按照依赖顺序执行它们的 <code>init()</code> 函数——如果你的包导入了 <code>fmt</code> ，那么 <code>fmt</code> （以及 <code>fmt</code> 所依赖的一切）会在你的包初始化之前完成初始化。</p>
<p>​	这也是包级变量初始化的时刻。因此，如果你在文件顶部有类似 <code>var db = connectToDB()</code> 的代码，它会在引导阶段运行——而不是在 <code>main()</code> 启动时。</p>
<p>现在，终于，运行时和你的代码之间再无任何隔阂。</p>
<h3 id="finally-your-main--最后你的-main-函数">Finally: Your main() ( 最后：你的 main() 函数)</h3>
<p>​	经历了这一切——汇编入口点、线程本地存储、CPU 检测、内存分配器、调度器、垃圾回收器、系统监视器、初始化函数——运行时终于调用了你的 <code>main</code> 函数。</p>
<p>但当它返回时会发生什么？</p>
<h3 id="after-main-returns-main-返回之后">After main() Returns main() 返回之后</h3>
<p>​	当你的 <code>main</code> 返回时，运行时并不会立即退出。它会给予一个短暂的宽限期，让那些正在处理恐慌的 goroutine 完成它们的延迟清理工作。但其他仍在运行的 goroutine 呢？它们会被直接终止——没有警告，没有清理。如果你需要它们完成，你必须显式地进行同步（例如，使用 <code>sync.WaitGroup</code> ）。</p>
<h1 id="总结">总结</h1>
<p>​	那么，回到我们最初的问题：为什么 Go 在“什么都不做”时显得“慢”？现在你明白了——它其实并非无所事事。当你的<code>main</code>函数开始运行时，运行时环境已经从零开始构建了完整的执行环境：第一个 goroutine 和线程、线程本地存储、栈池、内存分配器、映射的哈希函数、垃圾回收器、每个 CPU 核心对应一个 P 的调度器、系统监控线程，以及所有包的初始化函数。</p>
<p>​	这套机制相当复杂。但正是这些让 Go 语言在编写时显得如此轻松——协程之所以轻量，是因为栈池和分配器早已就位；内存管理之所以无感，是因为垃圾回收器已在后台运行；并发之所以能自然运作，是因为调度器在你的第一行代码执行前就已准备就绪。</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
